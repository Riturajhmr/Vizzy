// Plugin registration entry point.
// Each import triggers plugin self-registration via register() in registry.ts.
// Adding a new workflow = one new import line here. Zero other files change.
//
// Phase 1A: no plugins registered yet.
// Phase 3 will add: import './image-generation'
